kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
name: sandlock
nodes:
  - role: control-plane
  - role: worker
    # Uncomment to enable gVisor (M6):
    # kubeadmConfigPatches:
    #   - |
    #     kind: JoinConfiguration
    #     nodeRegistration:
    #       criSocket: /run/containerd/containerd.sock
